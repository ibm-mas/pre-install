---
kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
namespace: mas-dev1-core
resources:
  - ../../../maximo-operator-catalog/v9-251127-amd64/ibm-mas/9.1
  - ../../../maximo-operator-catalog/v9-251127-amd64/ibm-truststore-mgr
patches:
  - target:
      kind: Namespace
    patch: |-
      - op: replace
        path: /metadata/name
        value: mas-dev1-core

  - target:
      kind: OperatorGroup
      name: ibm-mas-operator-group
    patch: |-
      - op: replace
        path: /spec/targetNamespaces
        value:
          - mas-dev1-core
